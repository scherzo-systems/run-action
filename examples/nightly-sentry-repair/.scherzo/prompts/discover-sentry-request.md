Discover the bounded nightly issue set now. Use Sentry's authenticated API, preserve only
the four schema-declared fields for each selected issue, and submit the resulting
`{"include":[...]}` object through the structured result mechanism.
